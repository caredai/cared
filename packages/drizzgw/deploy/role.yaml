apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: drizzgw-gateway-pods
rules:
  # Manage Drizzle Gateway pods in K8S_NAMESPACE (same namespace as drizzgw workloads).
  - apiGroups: ['']
    resources: ['pods']
    verbs: ['get', 'list', 'watch', 'create', 'patch', 'delete']
